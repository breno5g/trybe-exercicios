# SELECT 'Olá, bem-vindo ao SQL!';
# SELECT 10;
# SELECT now();
# SELECT 20 * 2;
# SELECT 50 / 2;
# SELECT 18 AS idade;
# SELECT 2019 AS ano;
# SELECT 'Rafael', 'Martins', 25, 'Desenvolvedor Web';
# SELECT 'Rafael' AS nome, 'Martins' AS sobrenome, 25 AS idade, 'Desenvolvedor Web' AS 'Área de atuação';

# SELECT 'Breno' AS name, 'Santos' AS lastname, 'São Paulo' AS 'city', 22 AS age;
# SELECT 18 * 8;
# SELECT now() AS 'Data Atual';

# USE sakila;
# SELECT * FROM sakila.city;
# SELECT city_id, city, country_id  FROM city;

# SELECT * FROM city;
# SELECT first_name, last_name FROM customer;

# SELECT * FROM rental;
# SELECT title, description, release_year FROM film;

# SELECT * FROM sakila.nome_da_tabela