# SELECT CONCAT(first_name, ' ', last_name) AS 'Full Name' FROM sakila.actor;

USE sakila;

# SELECT CONCAT(title, ' ', release_year) AS 'Movie Release' FROM film;

# SELECT CONCAT(title, ' ', rating) AS 'Classification' FROM film;

SELECT CONCAT(address, ' ', district) AS 'Address' FROM address;